module.exports = {
	name: 'template',
	description: 'template',
	execute(message, args) {
		const cmdFile = require(`../../functions/embedFile.js`);
		//cmdFile.vectorEmbed(message, embedName, embedField, picName, embedField1, embedFieldTF);
		cmdFile.vectorEmbed(message, "", "", "", "", "false");
	},
};
